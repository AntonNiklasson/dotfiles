#! /bin/bash

echo "Installing..."


# link the files
for f in ~/.dotfiles/link/.*; do
	if test -f "$f"
	then
		echo "Linking ~/$(basename $f)"
		#ln -s "$f" "~($(basename $f)"
	fi
done


echo "Installation complete."
